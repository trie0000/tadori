node_modules/
*.local
.DS_Store

# dist/ はコミットする (業務PCで npm/ビルド不可のため、ビルド済み成果物を同梱)。
# ソースマップだけは除外。
dist/*.map

# 秘密情報を含む relay 設定 (env.example のみコミット)
scripts/*.env
*.env
!*.env.example

# pdf.js worker は build.js が生成 (node_modules から焼き込み)
